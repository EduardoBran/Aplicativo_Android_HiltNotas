<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HiltNotas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HiltNotas" parent="Base.Theme.HiltNotas" />

    <!-- Overlay específico para toolbars -->
    <style name="ToolbarOverlay" parent="ThemeOverlay.MaterialComponents.Toolbar.Primary">
        <!-- garante que a navigation icon (seta) seja tintada de branco -->
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextColor">@color/white</item>
        <item name="navigationIconTint">@color/white</item>
        <item name="iconTint">@color/white</item>
    </style>
</resources>